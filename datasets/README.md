# Datasets

See [`REGISTRY.md`](REGISTRY.md) for URLs, licenses, and **real SHA-256 digests** (filled 2026-09-17 after Hugging Face download).

- Binaries: gitignored `cache/` (preferred) or `data/`.
- Download helper: [`scripts/download_sift1m.sh`](scripts/download_sift1m.sh) (HF → FTP).
- Synthetic tiny fixtures: [`synthetic/`](synthetic/).
