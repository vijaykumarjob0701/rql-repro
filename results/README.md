# Results drop zone

**Date:** 2026-09-17 (Europe/Dublin)

Drop real run artifacts here (metrics JSON, ENV.txt, plans). Do not invent P0 numbers.

## Committed fixtures (small text/JSON only)

See [`fixtures/`](fixtures/) for copies of offline smoke summaries from the research repo E2E / validate logs — evidence that the compile stack ran, **not** ANN recall tables.

## Scratch (gitignored)

`scratch/` and `e2e/` are produced by local test / pipeline runs and are gitignored.
